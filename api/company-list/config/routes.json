{
  "routes": [
    {
      "method": "GET",
      "path": "/company-lists",
      "handler": "company-list.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/company-lists/count",
      "handler": "company-list.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/company-lists/:id",
      "handler": "company-list.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/company-lists",
      "handler": "company-list.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/company-lists/:id",
      "handler": "company-list.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/company-lists/:id",
      "handler": "company-list.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
